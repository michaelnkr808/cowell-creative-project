# Minimal dependencies for deployment
# Only includes what we actually use

# Web Framework
fastapi==0.115.0
uvicorn==0.32.0

# Environment Variables
python-dotenv==1.0.1

# Google Generative AI (for Gemini API)
google-generativeai>=0.8.0

# Security (required by Google APIs)
cryptography>=41.0.0
